 /** 
    function fun_one(): never {             // here if we have to use infinite then we need to go for never datatype
        while (true) {
            //console.log("Hello")            // here we iteration infinite times 
        }
    }  
    //fun_one();
*/